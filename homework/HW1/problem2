import gurobipy as gp

m = gp.Model()

x1 = m.addVar(name='x1')
x2 = m.addVar(name='x2')
h = m.addVar(name='h')
p = m.addVar(name='p')

C0 = m.addConstr(x1+2*x2<=20000)
C1 = m.addConstr(2*x1+3*x2<=35000)
C2 = m.addConstr(p<=200*h +1000)
C3 = m.addConstr(p<=3*x1+5*x2)

m.setObjective(5*p - 7*x1 - 12*x2 - 100*h, gp.GRB.MAXIMIZE)

m.optimize()

print(x1,x2,h,p)